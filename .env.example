# Optional. The app runs without this key and falls back to planning data.
# Add keys only locally, never commit the real .env file.
GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here
MODEL_NAME=gemini-2.5-flash
